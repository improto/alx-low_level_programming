#include <stdio.h>

/**
 * Main function: Output a sentence
 * (Description: It uses the printf() function to pront a senyence )
 * Return Value : 0
 * */
int main(void){
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
